---
const { title } = Astro.props
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="AI Character Video Creation Guide: Step-by-step, beginner-friendly workflow for making AI-driven character videos on macOS." />
    <meta property="og:title" content="AI Character Video Creation Guide" />
    <meta property="og:description" content="Step-by-step, beginner-friendly workflow for making AI-driven character videos on macOS." />
    <meta property="og:type" content="article" />
    <meta property="og:image" content="/favicon.svg" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
    <style>
      html { box-sizing: border-box; scroll-behavior: smooth; }
      *, *:before, *:after { box-sizing: inherit; }
      body {
        margin: 0;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
        background: #f6f7f9;
        color: #222;
        line-height: 1.7;
        font-size: 1.1rem;
        min-height: 100vh;
      }
      header {
        position: fixed;
        top: 0; left: 0; right: 0;
        background: rgba(255,255,255,0.85);
        border-bottom: 1px solid #e5e7eb;
        z-index: 100;
        height: 60px;
        display: flex;
        align-items: center;
        padding: 0 1.5rem;
        backdrop-filter: blur(8px);
        box-shadow: 0 2px 8px 0 rgba(0,0,0,0.03);
      }
      header h1 {
        font-size: 1.45rem;
        font-weight: 600;
        margin: 0;
        flex: 1;
        letter-spacing: -0.01em;
      }
      nav {
        display: flex;
        gap: 1.2rem;
      }
      nav a {
        color: #222;
        text-decoration: none;
        font-weight: 500;
        padding: 4px 0;
        border-bottom: 2px solid transparent;
        transition: color 0.15s, border-bottom 0.15s;
      }
      nav a:hover {
        color: #0071e3;
        border-bottom: 2px solid #0071e3;
      }
      main {
        margin: 84px auto 0 auto;
        max-width: 760px;
        padding: 2.5rem 1.5rem 2.5rem 1.5rem;
        background: #fff;
        border-radius: 18px;
        box-shadow: 0 4px 32px 0 rgba(0,0,0,0.06);
        min-height: 70vh;
      }
      main h1, main h2, main h3 {
        font-weight: 600;
        color: #111;
        margin-top: 2.2em;
        margin-bottom: 0.7em;
        letter-spacing: -0.01em;
      }
      main h1 { font-size: 2.1rem; }
      main h2 { font-size: 1.45rem; }
      main h3 { font-size: 1.15rem; }
      .toc {
        margin: 1.5rem 0 2.5rem 0;
        padding: 1.1rem 1.2rem;
        background: #f8fafc;
        border-radius: 10px;
        font-size: 1.05rem;
        box-shadow: 0 1px 4px 0 rgba(0,0,0,0.03);
      }
      .toc ul { margin: 0; padding-left: 1.2em; }
      .toc li { margin: 0.2em 0; }
      a { color: #0071e3; text-decoration: underline; }
      a:hover { color: #005bb5; }
      hr {
        border: none;
        border-top: 1px solid #e5e7eb;
        margin: 2.5rem 0;
      }
      footer {
        text-align: center;
        color: #888;
        font-size: 0.98rem;
        margin: 2.5rem 0 1.2rem 0;
        padding-top: 1.5rem;
        border-top: 1px solid #e5e7eb;
      }
      @media (max-width: 800px) {
        main { max-width: 100vw; border-radius: 0; box-shadow: none; }
      }
    </style>
  </head>
  <body>
    <header>
      <h1>AI Character Video Creation Guide</h1>
      <nav aria-label="Sections">
        <a href="#part-1--quickstart-overview">Part 1</a>
        <a href="#part-2--stepbystep-learning-roadmap">Part 2</a>
        <a href="#part-3--minimal-macosfriendly-hardware">Part 3</a>
        <a href="#part-4--prompt-cheatsheet-character-consistency">Part 4</a>
        <a href="#part-5--first-project-30second-pilot-video">Part 5</a>
      </nav>
    </header>
    <main>
      <slot name="toc" />
      <slot />
    </main>
    <footer>
      &copy; {new Date().getFullYear()} AI Character Video Guide. Built with Astro.
    </footer>
  </body>
</html> 